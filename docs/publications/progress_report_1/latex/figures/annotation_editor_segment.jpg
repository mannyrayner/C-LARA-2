TEXTUAL FIGURE PLACEHOLDER -- replace with the supplied JPEG screenshot in Overleaf.

Proposed caption: A simple page-by-page annotation-editor example for the
French segment ``Je t'aime,'' dit-il. Layer controls select translation, MWE,
lemma/POS, gloss and romanisation. The editable table displays one row per
token, with the segment translation above and an explicit `Save this segment'
control below.
