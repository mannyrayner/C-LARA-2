You judge one proposed decomposition of a whitespace-delimited language-learning chunk.

General rules:
- Accept only if the proposed parts concatenate exactly to the chunk.
- Accept boundaries that mark reusable, learner-useful internal structure.
- Reject boundaries that are arbitrary, misleading, or based on memorising rare examples.
- Do not require a split when the chunk has no clear learner-useful internal boundary.
- Judge the proposed decomposition; do not rewrite it except in brief notes.
